// TESTE NÃO PASSOU
import { CadastroBaseModule } from './cadastro-base.module';

describe('CadastroBaseModule', () => {
  let cadastrobaseModule: CadastroBaseModule;

  beforeEach(() => {
    cadastrobaseModule = new CadastroBaseModule();
  });

  // it('should create an instance', () => {
  //   expect(cadastrobaseModule).toBeTruthy();
  // });
});
